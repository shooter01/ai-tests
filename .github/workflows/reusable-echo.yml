name: Reusable Echo

on:
  workflow_call:
    inputs:
      message:
        description: "Строка, которую нужно вывести"
        required: true
        type: string

jobs:
  echo-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Message: ${{ inputs.message }}"
